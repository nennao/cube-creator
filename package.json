{
  "name": "rubiks-cube",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "build": "parcel build --dist-dir deploy/dist",
    "deploy": "gh-pages -d deploy/dist"
  },
  "dependencies": {
    "gl-matrix": "^3.4.3"
  },
  "devDependencies": {
    "@parcel/transformer-glsl": "^2.8.3",
    "gh-pages": "^5.0.0",
    "parcel": "^2.8.3"
  }
}
